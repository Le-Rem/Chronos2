{
  "name": "io.cordova.hellocordova",
  "displayName": "HelloCordova",
  "version": "1.0.0",
  "description": "A sample Apache Cordova application that responds to the deviceready event.",
  "main": "index.js",
  "scripts": {
    "run": "cordova run",
    "buildTSC": "tsc",
    "buildCSS": "sass --watch ./src/scss/index.scss ./www/css/index.css",
    "build": "npm run buildCSS && npm run buildTSC",
    "buildRun": "npm run build && npm run run"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova-android": "^10.1.2",
    "cordova-browser": "^6.0.0",
    "typescript": "^4.9.4"
  },
  "cordova": {
    "platforms": [
      "browser",
      "android"
    ]
  }
}